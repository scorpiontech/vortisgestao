# Bloco Nginx para expor o Supabase self-hosted via HTTPS.
# Copie para /etc/nginx/sites-available/supabase, ative com ln -s para sites-enabled
# e rode `sudo certbot --nginx -d supabase.vortisgestao.com.br` para o certificado.

server {
    listen 80;
    listen [::]:80;
    server_name supabase.vortisgestao.com.br;

    # Para o Certbot (HTTP-01)
    location /.well-known/acme-challenge/ {
        root /var/www/html;
    }

    # Redireciona o resto para HTTPS (o Certbot vai injetar isso, mas deixamos pronto)
    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name supabase.vortisgestao.com.br;

    # Os certificados são preenchidos pelo Certbot:
    # ssl_certificate     /etc/letsencrypt/live/supabase.vortisgestao.com.br/fullchain.pem;
    # ssl_certificate_key /etc/letsencrypt/live/supabase.vortisgestao.com.br/privkey.pem;

    # Permite uploads grandes (Storage, certificado A1, etc.)
    client_max_body_size 50M;

    # Timeouts mais generosos para Realtime/Edge Functions
    proxy_read_timeout 3600s;
    proxy_send_timeout 3600s;

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_http_version 1.1;
        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # WebSocket (Realtime)
        proxy_set_header Upgrade    $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}
